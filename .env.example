# Supabase Configuration (Frontend)
VITE_SUPABASE_URL=your_supabase_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# IMPORTANT: DO NOT use SERVICE_ROLE_KEY in frontend!
# SERVICE_ROLE_KEY should ONLY be used in backend/server code

# Gemini AI Configuration
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# N8N Webhooks Configuration
# La app llama a webhooks de n8n para procesamiento pesado (recomendaciones, pagos, etc.)
VITE_N8N_BASE_URL=https://n8nskinity-n8n.ixbbes.easypanel.host

# FUTURE: Stripe Configuration (Managed by N8N)
# Estos valores se usarán cuando se implemente el webhook de Stripe en n8n.
# Actualmente NO se usan en la app. Los pagos se gestionarán vía n8n webhooks.
# VITE_STRIPE_PUBLIC_KEY=your_stripe_public_key_here
# VITE_STRIPE_ANNUAL_PRICE_ID=your_annual_price_id
# VITE_STRIPE_ANNUAL_PRODUCT_ID=your_annual_product_id
# VITE_STRIPE_MONTHLY_PRICE_ID=your_monthly_price_id
# VITE_STRIPE_MONTHLY_PRODUCT_ID=your_monthly_product_id

# IMPORTANT: DO NOT use STRIPE_SECRET_KEY in frontend!
# STRIPE_SECRET_KEY should ONLY be used in backend/server code (N8N)

# Note: All VITE_ prefixed variables will be exposed in the client bundle.
# Never put sensitive keys (SERVICE_ROLE, SECRET_KEY) with VITE_ prefix!
